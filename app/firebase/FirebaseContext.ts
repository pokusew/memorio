"use strict";

import React from 'react';


// @ts-ignore
const FirebaseContext = React.createContext<any>(undefined);

FirebaseContext.displayName = 'FirebaseContext';

export default FirebaseContext;
